Place **grafana_cloud_rw** here: one line, remote_write token (Cloud Access Policy with `metrics:write`).

```bash
printf '%s' 'glc_...' > grafana_cloud_rw && chmod 600 grafana_cloud_rw
```

Git ignores `grafana_cloud_rw`; do not commit it.
